ultralytics==8.3.*
torch==2.4.*
torchvision==0.19.*
mlflow==2.16.*
fastapi==0.115.*
uvicorn[standard]==0.30.*
pillow==10.4.*
numpy==1.26.*
opencv-python-headless==4.10.*   # for the API/Docker (no GUI needed on servers).
# For models/realtime_inference.py's local cv2.imshow() window, swap this for
# `pip install opencv-python` instead (same API, but with GUI support) —
# don't install both, they conflict.
pydantic==2.9.*
python-multipart==0.0.*
pytest==8.3.*
httpx==0.27.*
scikit-learn==1.5.*
